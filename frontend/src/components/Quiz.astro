---
// Quiz stage — since 08.09 four workshop photos, one per step, crossfading
// as the sofa comes together: frame → foam filling → upholstery → finished
// sofa. This is the client's own idea (his question about the old scene:
// «незаполнены картинки, какая идея» → «каркас, наполнение, полуобивка,
// целый диван»). Step 3 currently shows the sewing photo; the client has
// not yet sent a half-upholstered frame — swap the src when it arrives.
// Step 4 (since 09.09) is the client's finished order shot in the factory
// photo zone, sent for exactly this step («именно с фотозоны, не в
// интерьер»); the same photo closes the Production strip. It replaced
// model-01, which left the page. Zoom/light state is still data-lit="1".."4" on
// .qz__scene set by quiz.js's paint(); only the CSS it drives changed.
//
// Previous scene, kept for history — "fabric to sofa" (variant B), chosen by the user 27.08 from
// five live mockups in clients/sf-sofa/opt-site-mockups/quiz-scene-variants.html
// (the SVG assembly scene and an interim brightness-ramp scene were both
// rejected before this). Photo: model-01.webp (ivory herringbone sectional) —
// of the 12 showcase photos it has the highest native resolution (1280px),
// which the mockup review confirmed stays sharp under a ~4.4x CSS zoom; its
// herringbone weave also reads as a strong, legible texture at that
// magnification. Mechanic: step 1 is an extreme close-up on the fabric
// (transform: scale, no crop/zoom framework), each step zooms out and
// brightens, step 4 is near-full, finish() leaves the scene on the step-4
// (near-full) state since finish() never re-paints the scene. Zoom/light state
// is driven by data-lit="1".."4" on .qz__scene, set by quiz.js's paint() —
// same attribute/mechanism as the two earlier scenes, only the CSS the
// attribute drives has changed. Behavior lives in ../scripts/quiz.js.
---

<div class="qz" id="qz">
	<div class="qz__stage">
		<div class="qz__scene" id="qzScene" data-lit="1">
			<img src="/ceh/karkasy.webp" alt="Собранные каркасы дивана с эластичными ремнями" loading="eager" fetchpriority="high" width="720" height="960" />
			<img src="/ceh/pena.webp" alt="Формовка пеной — заготовка будущего дивана" loading="lazy" width="720" height="960" />
			<img src="/ceh/poshiv.webp" alt="Пошив обивки на промышленных машинах" loading="lazy" width="720" height="960" />
			<img src="/ceh/gotovo.webp" alt="Готовый модульный диван в фотозоне фабрики" loading="lazy" width="720" height="960" />
		</div>
		<p class="qz__cap" id="qcap" aria-live="polite">Шаг 1 из 4 · каркас</p>
	</div>
	<div class="prg"><i class="on"></i><i></i><i></i><i></i></div>

	<div class="qz__body" id="qbody">
		<div class="step" data-s="1">
			<p class="qz__meta">Вопрос 1 из 4</p>
			<p class="qz__q" tabindex="-1">Что вы продаёте сейчас?</p>
			<button class="opt" type="button">Мебель в своём салоне или шоуруме</button>
			<button class="opt" type="button">Мебель на маркетплейсах или в интернет-магазине</button>
			<button class="opt" type="button">Комплектую объекты: отели, апарты, рестораны</button>
			<button class="opt" type="button">Я дизайнер или архитектор, привожу клиентов</button>
		</div>
		<div class="step" data-s="2" hidden>
			<p class="qz__meta">Вопрос 2 из 4</p>
			<p class="qz__q" tabindex="-1">Какой у вас масштаб?</p>
			<button class="opt" type="button">Одна точка или площадка</button>
			<button class="opt" type="button">От двух до пяти</button>
			<button class="opt" type="button">Больше пяти или сеть</button>
			<button class="opt" type="button">Только открываюсь</button>
		</div>
		<div class="step" data-s="3" hidden>
			<p class="qz__meta">Вопрос 3 из 4</p>
			<p class="qz__q" tabindex="-1">В каком городе работаете?</p>
			<div class="fld"><label for="q3">Город</label><input id="q3" placeholder="Ростов-на-Дону" autocomplete="address-level2"></div>
		</div>
		<div class="step" data-s="4" hidden>
			<p class="qz__meta">Последний шаг</p>
			<p class="qz__q" tabindex="-1">Куда направить оптовое предложение?</p>
			<div class="fld"><label for="q4a">Имя</label><input id="q4a" placeholder="Как к вам обращаться" autocomplete="name"></div>
			<div class="fld"><label for="q4b">Телефон или мессенджер</label><input id="q4b" type="tel" placeholder="+7 …" autocomplete="tel" inputmode="tel"></div>
			<label class="qz__consent">
				<input type="checkbox" id="qzConsent">
				<span>Даю <a href="/soglasie">согласие на обработку персональных данных</a> и ознакомлен с <a href="/politika">политикой обработки персональных данных</a></span>
			</label>
			<p class="qz__err" id="qzError" role="alert" hidden></p>
		</div>
		<div class="qz__nav">
			<button class="ghost" type="button" id="qback" disabled>Назад</button>
			<button class="cta" type="button" id="qnext">Дальше</button>
		</div>
	</div>

	<div class="qz__res" id="qres">
		<h3>Диван собран</h3>
		<p>Заявка ушла менеджеру — он свяжется с вами и направит предложение под ваш запрос.</p>
	</div>
</div>

<style>
	.qz { background: var(--panel); border: 1px solid var(--line); position: relative; display: flex; flex-direction: column; }
	.qz__body { flex: 1 1 auto; } /* the panel stretches to the left column's height; spare space goes below the buttons */
	.qz__stage { border-bottom: 1px solid var(--line); padding: 18px 20px 8px; background: linear-gradient(180deg,#161411,#1B1916); }
	.qz__scene { position: relative; aspect-ratio: 4 / 3; overflow: hidden; background: #000; }
	@media (orientation: landscape) and (max-height: 560px) {
		.qz__scene { aspect-ratio: auto; height: 60vh; height: 60svh; }
	}
	.qz__scene img {
		position: absolute;
		inset: 0;
		width: 100%;
		height: 100%;
		object-fit: cover;
		object-position: 50% 55%;
		display: block;
		opacity: 0;
		transition: opacity var(--t) ease;
	}
	/* The photo-zone shot is portrait with the sofa in its upper half and bare
	   floor below; the 4:3 window sits higher so the sofa is not cut at the top. */
	.qz__scene img:nth-child(4) { object-position: 50% 40%; }
	.qz__scene[data-lit="1"] img:nth-child(1),
	.qz__scene[data-lit="2"] img:nth-child(2),
	.qz__scene[data-lit="3"] img:nth-child(3),
	.qz__scene[data-lit="4"] img:nth-child(4) { opacity: 1; }
	.qz__cap { font-family: var(--mono); font-size: 10.5px; letter-spacing: .12em; text-transform: uppercase; color: var(--grey-dim); text-align: center; padding: 8px 0 12px; margin: 0; }
	.qz__body { padding: 22px 20px 26px; }
	.qz__meta { font-family: var(--mono); font-size: 10px; letter-spacing: .11em; text-transform: uppercase; color: var(--grey-dim); margin: 0 0 10px; }
	.qz__q { font-weight: 600; font-size: clamp(17px,2.1vw,21px); margin: 0 0 16px; line-height: 1.25; color: var(--ivory); }
	.qz__nav { display: flex; gap: 12px; align-items: center; flex-wrap: wrap; margin-top: 6px; }
	.qz__res { display: none; padding: 30px 20px 34px; text-align: center; }
	.qz__res.on { display: block; }
	.qz__res h3 { font-family: var(--serif); font-weight: 400; font-size: 22px; margin: 0 0 10px; color: var(--ivory); }
	.qz__res p { color: var(--grey); font-size: 14px; max-width: 40ch; margin: 0 auto; }

	.qz__consent { display: flex; gap: 12px; align-items: flex-start; cursor: pointer; margin: 4px 0 14px; }
	.qz__consent input {
		appearance: none;
		-webkit-appearance: none;
		width: 18px;
		height: 18px;
		flex: 0 0 18px;
		border: 1px solid var(--line-ui);
		background: transparent;
		margin-top: 2px;
		cursor: pointer;
		position: relative;
		transition: border-color var(--t) ease, background var(--t) ease;
	}
	.qz__consent input:checked { background: var(--ivory); border-color: var(--ivory); }
	.qz__consent input[aria-invalid="true"] { border-color: var(--ivory); }
	.qz__consent input:checked:after {
		content: '';
		position: absolute;
		left: 5px;
		top: 1px;
		width: 5px;
		height: 9px;
		border: solid var(--bg);
		border-width: 0 2px 2px 0;
		transform: rotate(45deg);
	}
	.qz__consent span { color: var(--grey); font-size: 13.5px; line-height: 1.5; }
	.qz__consent a { color: var(--ivory); text-decoration: underline; text-underline-offset: 2px; }
	.qz__err { color: var(--ivory); font-size: 12px; margin: 0 0 10px; border-left: 2px solid var(--ivory); padding-left: 10px; }

	@media (max-width: 470px) {
		.qz__nav .cta { width: auto; flex: 1 1 auto; }
	}
	@media (prefers-reduced-motion: reduce) {
		.qz__consent input { transition: none; }
		.qz__scene img { transition: none; }
	}
</style>

<script>
	import '../scripts/quiz.js';
</script>
